#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

ll a , b;

int main(){
    cin >> a >> b;

    ll delta = b - a + 1;

    ll sum = 0;
    for(int bit = 0; pow(2, bit) <= b; bit++){
        if ()
    }
    
    cout << bits << endl;

    
}