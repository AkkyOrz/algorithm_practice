#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

vector<string> maze[810][810];// maze[h][w]

int h, w;
int k;

int bfs()
int main(){

    cin >> h >> w;
    cin >> k;

    for (int i = 0; i < h; i++){
        cin >> maze[i];
    }

    //初動で行けるところに行った後に、後何回移動すればいいかを考える


}